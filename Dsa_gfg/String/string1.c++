#include<bits/stdc++.h>
using namespace std;
int main()
{
    char ch;
    cin>>ch;
    if((ch>=97 && ch<=122)||(ch>=65 && ch<=90))
    {
        cout<<"YES ! It's a character !!!\n";
    }
    else
    {
        cout<<"NO ! It's  not a character !!!\n";
    }
return 0;
}