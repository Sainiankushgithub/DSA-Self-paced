// REMOVE THE DUPLICATE FROM THE SORTED ARRAY in sorted way
#include<bits/stdc++.h>
using namespace std;
int removeduplicate(int arr[],int n)
{
    int res=1;
    int temp[n];
    for(int i=0;i<n;i++)
    {

    }
}
int main()
{
    int n;
    cout<<"Enter the size of an array\n";
    cin>>n;
    int arr[n];
    cout<<"Enter the numbers of an elements of an array\n";
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    removeduplicate(arr,n);
return 0;
}