// ADJACENCY IN LIST UNDIRECTED GRAPH
#include<stdio.h>
#include<stdlib.h>
struct Node
{
    int data;
    struct Node*next;
};
struct Node*addEdge(struct Node*head,int vertex)
{
    struct Node*temp=(struct Node*)malloc(sizeof(struct Node));
    temp->data;
    temp->next=NULL;
}
int main()
{
    return 0;
}